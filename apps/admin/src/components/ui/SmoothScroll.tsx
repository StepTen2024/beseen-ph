'use client';

import { ReactNode } from 'react';

export default function SmoothScroll({ children }: { children: ReactNode }) {
  // Simplified version without lenis dependency
  return <>{children}</>;
}
